{
  "name": "intern-diary",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install && npm run build",
    "install-all": "cd client && npm install && cd ../server && npm install"
  },
  "author": "",
  "license": "ISC",
  "description": "A comprehensive platform for managing internship applications, tasks, and opportunities"
}
